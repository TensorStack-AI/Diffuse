<Views:ViewBase x:Class="Diffuse.Views.SettingsGeneralView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:Diffuse.Views"
    xmlns:CommonControls="clr-namespace:TensorStack.WPF.Controls;assembly=TensorStack.WPF"
    xmlns:Controls="clr-namespace:Diffuse.Controls"
    xmlns:Views="clr-namespace:Diffuse.Views">
    <GroupBox DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:SettingsGeneralView}}}" Margin="10, 20, 20, 20" Header="General Settings" Padding="4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0" VerticalAlignment="Top">
                <Button DockPanel.Dock="Bottom" Command="{Binding SaveCommand}" Width="120" Height="30" HorizontalAlignment="Right" Margin="0,10,0,0" >
                    <StackPanel Orientation="Horizontal">
                        <CommonControls:FontAwesome Icon="f0c7" IconStyle="Light" Color="{StaticResource InfoColour}"/>
                        <TextBlock Text="Save Changes" Margin="5,0,0,0"/>
                    </StackPanel>
                </Button>
                <UniformGrid Columns="1">
                    <StackPanel>
                        <TextBlock Text="Model Directory" Style="{StaticResource FieldTextBlockStyle}"/>
                        <CommonControls:FilePickerTextBox FileName="{Binding Settings.DirectoryModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsFolderPicker="True" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="HuggingFace Model Cache" Style="{StaticResource FieldTextBlockStyle}"/>
                        <CommonControls:FilePickerTextBox FileName="{Binding Settings.DirectoryCache, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsFolderPicker="True" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="HuggingFace Access Token" Style="{StaticResource FieldTextBlockStyle}"/>
                        <TextBox  Text="{Binding Settings.SecureToken}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Temp Directory" Style="{StaticResource FieldTextBlockStyle}"/>
                        <CommonControls:FilePickerTextBox FileName="{Binding Settings.DirectoryTemp, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsFolderPicker="True" />
                    </StackPanel>
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Right" Margin="8,0,0,0" Width="120">
                            <TextBlock Text="Max History" Style="{StaticResource FieldTextBlockStyle}"/>
                            <TextBox  Text="{Binding Settings.MaxHistory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  />
                        </StackPanel>
                        <StackPanel >
                            <TextBlock Text="History Directory" Style="{StaticResource FieldTextBlockStyle}"/>
                            <CommonControls:FilePickerTextBox FileName="{Binding Settings.DirectoryHistory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsFolderPicker="True" />
                        </StackPanel>
                    </DockPanel>
                </UniformGrid>
            </DockPanel>
        </Grid>
    </GroupBox>
</Views:ViewBase>
