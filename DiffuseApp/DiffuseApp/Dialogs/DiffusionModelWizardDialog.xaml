<CommonControls:DialogControl x:Class="Diffuse.Dialogs.DiffusionModelWizardDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Diffuse.Dialogs"
        xmlns:CommonControls="clr-namespace:TensorStack.WPF.Controls;assembly=TensorStack.WPF"
        xmlns:Common="clr-namespace:Diffuse.Common"
        Title="Add Model"
        MinWidth="600"
        MaxWidth="600"
        Icon="/Images/Icon.ico"
        Style="{StaticResource {x:Type CommonControls:DialogControl}}">
    <DockPanel DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:DiffusionModelWizardDialog}}}" Margin="15, 15, 15, 10">

        <!--Settings-->
        <StackPanel DockPanel.Dock="Top" >

            <UniformGrid Columns="3" >
                <StackPanel Margin="0,0,2,0">
                    <TextBlock Text="Pipeline" Style="{StaticResource FieldTextBlockStyle}"/>
                    <ComboBox ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" DisplayMemberPath="Name"  />
                </StackPanel>
                <StackPanel Margin="0,0,2,0">
                    <TextBlock Text="Type" Style="{StaticResource FieldTextBlockStyle}"/>
                    <ComboBox ItemsSource="{Binding SelectedItem.Options}" SelectedItem="{Binding SelectedOption}" DisplayMemberPath="Name" />
                </StackPanel>
                <StackPanel Margin="2,0,2,0">
                    <TextBlock Text="Source" Style="{StaticResource FieldTextBlockStyle}"/>
                    <ComboBox x:Name="ComboBoxSource" ItemsSource="{Binding ModelSources}" SelectedItem="{Binding SelectedSource}" />
                </StackPanel>
            </UniformGrid>

            <Grid Margin="0,4,0,0">
                <StackPanel Visibility="{Binding SelectedItem, ElementName=ComboBoxSource, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Folder}">
                    <TextBlock Text="Model Folder (diffusers)" Style="{StaticResource FieldTextBlockStyle}" />
                    <CommonControls:FilePickerTextBox FileName="{Binding SelectedModelPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsFolderPicker="True" IsRequired="True" />
                </StackPanel>
                <StackPanel Visibility="{Binding SelectedItem, ElementName=ComboBoxSource, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=SingleFile}">
                    <TextBlock Text="Model Filename (safetensors)" Style="{StaticResource FieldTextBlockStyle}" />
                    <CommonControls:FilePickerTextBox FileName="{Binding SelectedModelPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsRequired="True" Filter="Model Files|*.safetensors;*.gguf;" DefaultExt="safetensors"/>
                </StackPanel>
                <StackPanel Visibility="{Binding SelectedItem, ElementName=ComboBoxSource, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=HuggingFace}">
                    <TextBlock Text="HuggingFace Url" Style="{StaticResource FieldTextBlockStyle}" />
                    <TextBox Text="{Binding SelectedModelPath, UpdateSourceTrigger=PropertyChanged}"  />
                </StackPanel>
                <StackPanel Visibility="{Binding SelectedItem, ElementName=ComboBoxSource, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Checkpoint}">
                    <StackPanel>
                        <TextBlock Text="HuggingFace Url (Base Repo)" Style="{StaticResource FieldTextBlockStyle}" />
                        <TextBox Text="{Binding SelectedModelPath, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Base Model (safetensors)" Style="{StaticResource FieldTextBlockStyle}" />
                        <CommonControls:FilePickerTextBox FileName="{Binding CheckpointModel.ModelCheckpoint, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Filter="Model Files|*.safetensors;*.gguf;" DefaultExt="safetensors"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Vae (safetensors)" Style="{StaticResource FieldTextBlockStyle}" />
                        <CommonControls:FilePickerTextBox FileName="{Binding CheckpointModel.VaeCheckpoint, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Filter="Model Files|*.safetensors;*.gguf;" DefaultExt="safetensors"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Text Encoder (safetensors)" Style="{StaticResource FieldTextBlockStyle}" />
                        <CommonControls:FilePickerTextBox FileName="{Binding CheckpointModel.TextEncoderCheckpoint, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Filter="Model Files|*.safetensors;*.gguf;" DefaultExt="safetensors"/>
                    </StackPanel>
                    <TextBlock Text="Any combination of checkpoint files is supported. Select at least one." FontSize="10" FontStyle="Italic" Opacity=".7" HorizontalAlignment="Center"  Margin="0,3,0,0"/>
                </StackPanel>
            </Grid>

            <StackPanel Margin="0,4,0,0">
                <TextBlock Text="Model Name" Style="{StaticResource FieldTextBlockStyle}"/>
                <TextBox Text="{Binding SelectedName, UpdateSourceTrigger=PropertyChanged}"  />
            </StackPanel>

        </StackPanel>


        <!--Save/Cancel/Errors-->
        <DockPanel DockPanel.Dock="Bottom" Height="30" Margin="0,15,0,0">
            <UniformGrid DockPanel.Dock="Right" Columns="2" >
                <Button Content="Save" Command="{Binding SaveCommand}" Width="100" IsDefault="True"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}"/>
            </UniformGrid>
            <ItemsControl ItemsSource="{Binding Errors}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="IndianRed" TextWrapping="Wrap" FontSize="11"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DockPanel>

        <TextBlock/>

    </DockPanel>
</CommonControls:DialogControl>