namespace DiffuseApp.Downloader
{
    public class Options
    {
        //[Option('s', "ServerConfig", Required = true, HelpText = "The server config, as JSON filename or JSON string")]
        //public string ServerConfig { get; set; }
    }
}
